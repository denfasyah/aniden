"use client";

/**
 * SessionProvider placeholder.
 * Will be replaced with next-auth/react SessionProvider in Step 5 (Auth Setup).
 */
export function SessionProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
